icon: material/arrow-expand-all

# :material-arrow-expand-all: Camera Zoom

The **CameraZoom** component, once attached to the camera, can zoom in the FOV when holding middle mouse.

If you are using [game settings](../scripting/etgamesettings.md), this component will automatically set and update the camera's field of view.